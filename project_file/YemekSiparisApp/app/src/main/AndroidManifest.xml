<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yazilimmuhendisligi.yemeksiparis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".adminMain"
            android:label="@string/title_activity_admin_main"></activity>
        <activity
            android:name=".musteri"
            android:label="@string/title_activity_musteri" />
        <activity
            android:name=".parolaUnuttum"
            android:parentActivityName=".girisActivity" />
        <activity
            android:name=".firmaKayit"
            android:label="Firma Kayıt"
            android:parentActivityName=".girisActivity" />
        <activity
            android:name=".musteriKayit"
            android:label="Müşteri Kayıt"
            android:parentActivityName=".girisActivity" />
        <activity android:name=".girisActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>